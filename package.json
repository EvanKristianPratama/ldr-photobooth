{
    "name": "ldr-photobooth",
    "version": "1.0.7",
    "description": "LDR Photobooth - P2P Photo Sharing",
    "scripts": {
        "dev:client": "cd client && npm run dev",
        "dev:worker": "cd worker && npx wrangler dev",
        "build:client": "cd client && npm run build",
        "install-all": "cd client && npm install && cd ../worker && npm install"
    },
    "keywords": [
        "photobooth",
        "webrtc",
        "p2p"
    ],
    "author": "",
    "license": "MIT",
    "engines": {
        "node": ">=18.0.0"
    },
    "devDependencies": {
        "@testing-library/dom": "^10.4.1",
        "@testing-library/react": "^16.3.0",
        "happy-dom": "^20.0.11"
    }
}
