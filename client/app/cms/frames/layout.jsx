export const metadata = {
  title: 'Frame CMS — LDR Photobooth',
  description: 'Create and manage frame templates for LDR Photobooth',
};

export default function CmsFramesLayout({ children }) {
  return children;
}
