window.addEventListener('DOMContentLoaded', () => {
  console.log('Electron Preload Script: DOM is fully loaded.');
  // Di sini Anda bisa memasukkan contextBridge API jika nantinya butuh komunikasi 
  // langsung antara frontend dan backend/sistem operasi asli macOS/Windows.
});
